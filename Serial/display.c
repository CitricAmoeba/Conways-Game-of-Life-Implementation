#include "stdio.h"

int main() {

  const int GRID_WIDTH = 15;
  const int GRID_HEIGHT = 15;

  //Create structure with malloc()
  //Initialise with - using nested-for loop
  int gen = 0;
  printf("Generation: %d", gen);
  
}
